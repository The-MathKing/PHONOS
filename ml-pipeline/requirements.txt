# SSI Neuro-Prosthetic — ML Pipeline Dependencies
# Optimized for Apple Silicon M4 (arm64) with Metal GPU acceleration
# Python 3.12 required for tensorflow-macos + tensorflow-metal compatibility

# ── Numerical & Scientific Core ────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0

# ── Apple Silicon GPU-Accelerated TensorFlow ───────────────────────────────────
# tensorflow-macos: Apple-patched TF build for arm64/M-series chips
# tensorflow-metal: Metal Performance Shaders backend for GPU ops (M1/M2/M3/M4)
tensorflow-macos>=2.16.0
tensorflow-metal>=1.1.0

# ── Notebook & Visualization ───────────────────────────────────────────────────
jupyterlab>=4.1.0
matplotlib>=3.8.0
